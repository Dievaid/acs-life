import {
    VStack,
    Text
} from "@chakra-ui/react";

export const TicketsView: React.FC = () => {
    return (
        // @ts-ignore
        <VStack>
            <Text>Tickets page</Text>
        </VStack>
    );
}