import {
    VStack,
    Text
} from "@chakra-ui/react";

export const SettingsView: React.FC = () => {
    return (
        // @ts-ignore
        <VStack>
            <Text>Settings page</Text>
        </VStack>
    );
}