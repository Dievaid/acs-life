import {
    VStack,
    Text
} from "@chakra-ui/react";

export const TimetablesView: React.FC = () => {
    return (
        // @ts-ignore
        <VStack>
            <Text>Timetables page</Text>
        </VStack>
    );
}