import {
    VStack,
    Text
} from "@chakra-ui/react";

export const CatalogView: React.FC = () => {
    return (
        // @ts-ignore
        <VStack>
            <Text>Catalog page</Text>
        </VStack>
    );
}