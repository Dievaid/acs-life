import {
    VStack,
    Text,
} from "@chakra-ui/react";

export const HomeView: React.FC = () => {
    return (
        // @ts-ignore
        <VStack>
            <Text>Home page</Text>
        </VStack>
    );
}